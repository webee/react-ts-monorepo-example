{
  "name": "@webee/demotopic-root",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "playgrounds/*",
    "documentations/*"
  ],
  "scripts": {
    "commit": "cz",
    "build": "lerna run build",
    "version": "lerna version --no-private",
    "publish": "lerna exec --no-private -- npm publish"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "commitizen": "^4.3.0",
    "cz-conventional-changelog": "^3.3.0",
    "lerna": "^6.6.0",
    "typescript": "^5.0.2"
  }
}
